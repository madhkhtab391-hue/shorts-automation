name: Create Kids Short - Arabic

on:
  workflow_dispatch:
    inputs: {}

jobs:
  create-short:
    runs-on: ubuntu-latest

    steps:
      - name: Get video from Cloudflare Worker
        run: |
          curl -L "https://orange-cherry-5bc8.madhkhtab391.workers.dev/pexels" -o videos.json

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y jq ffmpeg fonts-noto-core fonts-noto-extra
          python3 -m pip install --user edge-tts

      - name: Download video
        run: |
          VIDEO_URL=$(jq -r '.[0].videoUrl' videos.json)
          echo "Downloading video..."
          curl -L "$VIDEO_URL" -o original.mp4

      - name: Create Arabic script
        run: |
          cat > script.txt <<'EOF'
          هل تعرف أن بعض الديناصورات كانت أكبر من الحافلة؟
          تخيل ديناصورًا ضخمًا يمشي بجانبك!
          يا ترى، ما أكبر ديناصور عاش على الأرض؟
          EOF

      - name: Create Arabic voice
        run: |
          python3 -m edge_tts \
            --voice ar-JO-TaimNeural \
            --rate=-5% \
            --text "$(cat script.txt)" \
            --write-media voice.mp3

      - name: Create captions
        run: |
          cat > captions.ass <<'EOF'
          [Script Info]
          ScriptType: v4.00+
          PlayResX: 1080
          PlayResY: 1920

          [V4+ Styles]
          Format: Name, Fontname, Fontsize, PrimaryColour, SecondaryColour, OutlineColour, BackColour, Bold, Italic, Alignment, MarginL, MarginR, MarginV, Encoding
          Style: Default,Noto Sans Arabic,70,&H00FFFFFF,&H00FFFFFF,&H00000000,&H80000000,1,0,2,60,60,180,1

          [Events]
          Format: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text
          Dialogue: 0,0:00:00.00,0:00:05.00,Default,,0,0,0,,هل تعرف أن بعض الديناصورات كانت أكبر من الحافلة؟
          Dialogue: 0,0:00:05.00,0:00:10.00,Default,,0,0,0,,تخيل ديناصورًا ضخمًا يمشي بجانبك!
          Dialogue: 0,0:00:10.00,0:00:15.00,Default,,0,0,0,,يا ترى، ما أكبر ديناصور عاش على الأرض؟
          EOF

      - name: Create Short
        run: |
          ffmpeg -y \
            -i original.mp4 \
            -i voice.mp3 \
            -vf "scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,subtitles=captions.ass" \
            -map 0:v:0 \
            -map 1:a:0 \
            -t 15 \
            -c:v libx264 \
            -preset veryfast \
            -crf 23 \
            -c:a aac \
            -b:a 128k \
            -shortest \
            short.mp4

      - name: Check output
        run: |
          ls -lh short.mp4
          ffprobe -v error \
            -show_entries format=duration \
            -of default=noprint_wrappers=1 \
            short.mp4

      - name: Upload Short
        uses: actions/upload-artifact@v4
        with:
          name: arabic-kids-dinosaur-short
          path: short.mp4
